// tailwind.config.js
module.exports = {
  darkMode: "class", // critical — lets us toggle via a class instead of OS-only media query
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: { extend: {} },
  plugins: [],
};
